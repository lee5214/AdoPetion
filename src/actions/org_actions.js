import { firestore } from "../modules/firebase";const orgsRef = firestore.collection("orgs");const FETCH_ORGS = "fetchOrgs";const FETCH_ORG_DETAIL = "fetchORGDETAIL";//const fetchOrgs = region => async dispatch => {};const fetchOrgDetail = orgID => async dispatch => {  let detail = await orgsRef    .doc(`${orgID}`)    .get()    .then(doc => {      if (!doc.exists) {        console.log("no doc found");        return null;      }      return doc.data();    })    .catch(err => {      console.log("fetchOrgDetail err: ", err);    });  dispatch({    type: FETCH_ORG_DETAIL,    payload: detail  });};export { FETCH_ORG_DETAIL, fetchOrgDetail, FETCH_ORGS };