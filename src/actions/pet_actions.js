import { firestore } from "../modules/firebase";import { Location } from "expo";const FETCH_PETS = "fetchPets";const fetchPets = region => async dispatch => {  try {    const location = await Location.reverseGeocodeAsync({      latitude: region.latitude,      longitude: region.longitude    });    console.log("fetchPets/location", location);    let docRef = firestore.collection("pets").where("animalID", "==", "10000");    let tempArr = [];    let pet = await docRef      .get()      .then(querySnapshot => {        querySnapshot.forEach(doc => {          //tempArr.push(doc.data())          // doc.data() is never undefined for query doc snapshots          console.log(doc.id, " => ", doc.data());        });      })      .catch(err => {        console.log("Error getting document", err);      });    console.log("action/tempArr ", tempArr.length);  } catch (e) {    console.log(e);  }  dispatch({    type: FETCH_PETS,    payload: tempArr  });};export { FETCH_PETS, fetchPets };