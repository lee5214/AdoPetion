import { firestore } from "../modules/firebase";const FETCH_PETS_BY_ORG = "fetchPetsByOrg";const docRef = firestore.collection("pets");const fetchPetsByOrg = orgID => async dispatch => {  let temp = {};  try {    let query = docRef.where("orgID", "==", orgID).limit(10);    await query      .get()      .then(querySnapshot => {        querySnapshot.forEach(doc => {          temp[doc.id] = doc.data();        });      })      .catch(e => {        console.log("fetch pets by org err: ", e);      });  } catch (e) {    console.log(e);  }  dispatch({    type: FETCH_PETS_BY_ORG,    payload: temp,    orgID: orgID  });};const FETCH_NEARBY_PETS = fetch_nearby_pets;const fetchNearbyPets = geoArray => async dispatch => {    if (!geoArray.length) return {      type:FETCH_NEARBY_PETS,      payload:{}    };    let temp = {};    geoArray.map(geo => {      temp[geo.key] = [];      firestore        .collection("pets")        .where("orgID", "==", geo.key)        .get()        .then(querySnapshot => {          querySnapshot.forEach(doc => {            if (doc.exists) {              //console.log("doc found: ", geo.key, doc.id);              temp[geo.key].push(doc.data());            } else {              console.log("no doc: ", geo.key);            }          });        }) //temp.push(doc.data()))        .catch(err => console.log("firestore err: ", err));    });    console.log("temp", temp[267][0].age);    return temp;  };}export { FETCH_PETS_BY_ORG, fetchPetsByOrg };