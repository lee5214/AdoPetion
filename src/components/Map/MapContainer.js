import React, { Component } from "react";import { MapView } from "expo";import { Text, View } from "react-native";import mapDarkStyle from "../../assets/mapDarkStyle";import Icon from "react-native-vector-icons/MaterialIcons";class MapContainer extends Component {  render() {    console.log("radius", this.props.radius);    console.log("markerlist size", this.props.markersList.length);    let { markersList, region, radius, onRegionChangeComplete } = this.props;    return (      <MapView        // showsMyLocationButton={true}        showsPointsOfInterest={true}        minZoomLevel={4}        customMapStyle={mapDarkStyle}        provider={MapView.PROVIDER_GOOGLE}        style={styles.mapView}        region={region}        initialRegion={{          longitude: -100,          latitude: 37,          longitudeDelta: 0.04,          latitudeDelta: 0.09        }}        //onMapReady={e => console.log("Map is ready")}        onRegionChangeComplete={onRegionChangeComplete}      >        <MapView.Circle          center={{            latitude: region.latitude,            longitude: region.longitude          }}          radius={1000 * radius} //in meters          fillColor={"rgba(114,255,250,0.5)"}          strokeColor={"transparent"}          geodesic={true}        />        {markersList.map(marker => {          let [latitude, longitude] = marker.location;          return (            <MapView.Marker              key={`markersInSearchArea-${marker.key}`}              coordinate={{                latitude,                longitude              }}              //image={pawImg}            >              <View style={styles.markerCircle}>                <Icon name="pets" size={20} color={'white'} type="materialicons" />                {/*<Image source={pawImg} style={{ width: 10, height: 10 }} >*/}                {/*</Image>*/}              </View>              {/*<View style={styles.markerCircle}>*/}              {/*<View style={styles.markerPoint} />*/}              {/*</View>*/}              <MapView.Callout>                <View style={styles.calloutContainer}>                  <Text>{marker.key}</Text>                </View>                <View>description:yoyo</View>              </MapView.Callout>            </MapView.Marker>          );        })}        {this.props.children}      </MapView>    );  }}const styles = {  mapView: {    flex: 1,    justifyContent: "center",    alignItems: "center"  },  markerCircle: {    height: 25,    width: 25,    borderRadius: 5,    overflow: "hidden",    backgroundColor: "rgba(0,122,255,0.1)",    borderWidth: 1,    borderColor: "white",    alignItems: "center",    justifyContent: "center"  },  markerPoint: {    height: 20,    width: 20,    borderWidth: 3,    borderColor: "white",    borderRadius: 10,    overflow: "hidden",    backgroundColor: "#007AFF"  },  calloutContainer: {    backgroundColor: "red",    height: 50,    width: 100  }};export default MapContainer;