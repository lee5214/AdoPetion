import React, { Component } from "react";import { MapView } from "expo";import { View } from "react-native";import mapDarkStyle from '../../assets/mapDarkStyle';class MapContainer extends Component {  render() {    console.log("radius", this.props.radius);    console.log("markerlist", this.props.markersList);    let { markersList } = this.props;    return (      <MapView        // showsMyLocationButton={true}        showsPointsOfInterest={true}        minZoomLevel={4}        customMapStyle={mapDarkStyle}        provider={MapView.PROVIDER_GOOGLE}        style={styles.mapView}        region={this.props.region}        initialRegion={{          longitude: -100,          latitude: 37,          longitudeDelta: 0.04,          latitudeDelta: 0.09        }}        onMapReady={e => console.log("Map is ready")}        onRegionChangeComplete={this.props.onRegionChangeComplete}      >        <MapView.Circle          center={{            latitude: this.props.region.latitude,            longitude: this.props.region.longitude          }}          radius={1000 * this.props.radius} //in meters        />        {markersList.map(marker => {          console.log(marker);          return (            <MapView.Marker              key={`markersInSearchArea-${marker.key}`}              coordinate={{                latitude: marker.location[0],                longitude: marker.location[1]              }}              title={marker.key}              description={"bala boom"}            >              <View style={styles.markerCircle}>                <View style={styles.markerPoint} />              </View>            </MapView.Marker>          );        })}        {this.props.children}      </MapView>    );  }}const styles = {  mapView: {    flex: 1,    justifyContent: "center",    alignItems: "center"  },  markerCircle: {    height: 50,    width: 50,    borderRadius: 25,    overflow: "hidden",    backgroundColor: "rgba(0,122,255,0.1)",    borderWidth: 1,    borderColor: "rgba(0,112,255,0.3)",    alignItems: "center",    justifyContent: "center"  },  markerPoint: {    height: 20,    width: 20,    borderWidth: 3,    borderColor: "white",    borderRadius: 10,    overflow: "hidden",    backgroundColor: "#007AFF"  }};export default MapContainer;