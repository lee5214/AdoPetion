import React, { Component } from "react";import { View } from "react-native";import { MapView } from "expo";import Icon from "react-native-vector-icons/MaterialIcons";import OrgDetailCallout from "./OrgDetailCallout";import Spinner from "../../components/Common/Spinner";class Marker extends Component {  componentDidUpdate() {    if (this.props.calloutVisible) {      this.marker.showCallout();    }  }  render() {    //console.log('this.marker',this.marker)    let { marker, orgsDetailList,onDeselectMarker } = this.props;    let [latitude, longitude] = marker.location;    console.log("visible", this.props.visible, marker.key);    return (      <MapView.Marker        key={`markersInSearchArea-${marker.key}`}        ref={ref => {          this.marker = ref;        }}        coordinate={{          latitude,          longitude        }}        onDeselect={onDeselectMarker}        identifier={marker.key.toString()}        onPress={() => this.props.onMarkerPress(marker.key)}      >        <View style={styles.markerCircle}>          <Icon name="pets" size={20} color={"white"} type="materialicons" />        </View>        <MapView.Callout style={styles.calloutContainer}>          {orgsDetailList[marker.key] ? (            <OrgDetailCallout detail={orgsDetailList[marker.key]} />          ) : (            <Spinner size={"large"} color={"#000000"} />          )}        </MapView.Callout>      </MapView.Marker>    );  }}const styles = {  markerCircle: {    height: 25,    width: 25,    borderRadius: 5,    overflow: "hidden",    backgroundColor: "rgba(0,122,255,0.1)",    borderWidth: 1,    borderColor: "white",    alignItems: "center",    justifyContent: "center"  },  calloutContainer: {    width: 300,    height: 200,    padding: 10,    borderRadius: 20,    backgroundColor: "red"  }};export default Marker;