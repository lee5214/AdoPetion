import React, { Component } from "react";import { View, Animated } from "react-native";class Ball extends Component {  state = {    posAnim: new Animated.ValueXY(0, 0)  };  componentDidMount() {    Animated.spring(this.state.posAnim, {      toValue: { x: 200, y: 500 }    }).start();  }  render() {    return (      <Animated.View style={this.state.posAnim.getLayout()}>        <View          style={{            height: 60,            width: 60,            borderRadius: 30,            borderWidth: 30,            borderColor: "black"          }}        />      </Animated.View>    );  }}export default Ball;