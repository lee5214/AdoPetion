import { CLEAR_LIKED_JOBS, FETCH_JOBS } from "../actions";const INITIAL_STATE = {  results: []};export default function(state = INITIAL_STATE, action) {  switch (action.type) {    case FETCH_JOBS:      return action.payload;    case CLEAR_LIKED_JOBS:      return action.payload;    default:      return state;  }}