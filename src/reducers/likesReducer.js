import { LIKE_JOB } from "../actions";import { REHYDRATE } from "redux-persist/lib/constants";export default function(state = [], action) {  switch (action.type) {    case REHYDRATE:      return action.payload.likedJobs || []; // [] for first time app runs otherwise it will be undefined    case LIKE_JOB:      return [action.payload, ...state];    default:      return state;  }}