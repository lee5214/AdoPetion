import { LIKE_PET } from "../actions";import REHYDRATE from 'expo';export default function(state = [], action) {  switch (action.type) {    case REHYDRATE:     return action.payload.likedJobs || []; // [] for first time app runs otherwise it will be undefined    case LIKE_PET:      return [action.payload, ...state];    default:      return state;  }}