import React, { Component } from "react";import { Dimensions, Image, ScrollView, Text, View } from "react-native";import { connect } from "react-redux";const Screen_Width = Dimensions.get("screen").width;class MapDetail extends Component {  static navigationOptions = {    //header:null  };  onpenPetDetailContainer = () => {};  render() {    let { petsList } = this.props;    return (      <ScrollView>        {Object.keys(petsList).map(pet => {          console.log("petdetail: ", petsList[pet]);          let { animalID, name, sex, pictures } = petsList[pet];          return (            <View key={animalID} style={{ justifyContent: "flex-start" }}>              <Text>{name}</Text>              <Text>{sex}</Text>              <ScrollView horizontal pagingEnabled>                {pictures &&                  pictures.map((pic,index) => (                    <Image                      key={`${animalID}-pic-${index}`}                      style={{                        width: Screen_Width - 80,                        height: 200,                        marginLeft: 10,                        marginRight: 10                      }}                      source={{ uri: `${pic.fullsizeUrl}` }}                      onPress={this.openPetDetailContainer}                    />                  ))}              </ScrollView>            </View>          );        })}      </ScrollView>    );  }}const mapStateToProps = state => {  console.log("state", state.petsList);  return {    petsList: state.petsList  };};export default connect(mapStateToProps)(MapDetail);