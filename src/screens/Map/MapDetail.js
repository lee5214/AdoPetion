import React, { Component } from 'react';import { ScrollView, View } from 'react-native';import PetDetailList from '../../components/Common/PetDetailList';class MapDetail extends Component{  static navigationOptions = ({navigation})=>({    title: navigation.getParam('petObj').name,    titleFontSize: 30  })  render(){    let data = this.props.navigation.getParam('petObj')    let {      name,      pictures,      size,      age,      sex,      color,      mix,      sizeCurrent,      sizeUOM,      breed,      birthdate,      adoptionFee,      descriptionPlain,      status    } = data    return(      <View style={{flex:1,marginTop:64+44}}>        <ScrollView>        <PetDetailList petObj = {data} />        </ScrollView>      </View>    )  }}export default MapDetail