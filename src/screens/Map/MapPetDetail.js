import React, { Component } from "react";import { View } from "react-native";import PetDetail from "../../components/Common/PetDetail";import MaterialIcons from 'react-native-vector-icons/MaterialIcons';class MapPetDetail extends Component {  static navigationOptions = ({ navigation }) => ({    title: navigation.getParam("petObj").name,    headerLeft: (      <MaterialIcons        name="keyboard-arrow-left"        size={30}        type="materialicons"        style={{ paddingLeft: 20 }}        onPress={() => {          navigation.goBack()        }}      />    ),  });  /*toOrgDetail = () => {    const petObj = this.props.navigation.getParam("petObj");    this.props.navigation.navigate("mapOrgDetail",{orgID:petObj.orgID});  };*/  toOrgDetail = orgID => {    this.props.navigation.navigate("mapOrgDetail",{orgID:orgID});  };  render() {    const petObj = this.props.navigation.getParam("petObj");    const {      name,      pictures,      size,      age,      sex,      color,      mix,      sizeCurrent,      sizeUOM,      breed,      birthdate,      adoptionFee,      descriptionPlain,      status    } = petObj;    return (      <View style={{ flex: 1 }}>          <PetDetail petObj={petObj} toOrgDetail={this.toOrgDetail}/>      </View>    );  }}export default MapPetDetail;