import React, { Component } from "react";import { connect } from 'react-redux';import { View } from "react-native";import PetDetail from "../../components/Common/PetDetail";class MapPetDetail extends Component {  static navigationOptions = ({ navigation }) => ({    title: navigation.getParam("petObj").name,    //titleFontSize: 30  });  toOrgDetail = orgID => {    this.props.navigation.navigate("mapOrgDetail",{orgID:orgID});  };  render() {    const petObj = this.props.navigation.getParam("petObj");    const {      name,      pictures,      size,      age,      sex,      color,      mix,      sizeCurrent,      sizeUOM,      breed,      birthdate,      adoptionFee,      descriptionPlain,      status    } = petObj;    return (      <View style={{ flex: 1 }}>          <PetDetail petObj={petObj} toOrgDetail={this.toOrgDetail}/>      </View>    );  }}function mapStateToProps(state) {  return {    likedPets: state.likedPets  };}export default connect(mapStateToProps,null)(MapPetDetail);