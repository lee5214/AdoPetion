import React, { Component } from "react";import { connect } from "react-redux";import { View } from "react-native";import { Button } from "react-native-elements";import * as actions from "../../actions";import MaterialIcons from "react-native-vector-icons/MaterialIcons";class ReviewSetting extends Component {  static navigationOptions = ({ navigation }) => ({    title: "Setting",    headerLeft: (      <MaterialIcons        name="keyboard-arrow-left"        size={30}        type="materialicons"        style={{ marginLeft: 20 }}        onPress={() => {          navigation.goBack();        }}      />    )  });  onLogout = () => {    console.log("log out");    this.props.facebookLogout();    this.props.navigation.navigate("welcome"); //navigate("back");  };  clearLikedPets = () => {    this.props.clearLikedPets();    this.props.navigation.navigate("reviewWrapper");  };  render() {    return (      <View        style={{          paddingTop: 100,          flex: 1,          justifyContent: "center",          alignItems: "center"        }}      >       {/* <Text>Token: {this.props.token}</Text>*/}        <Button          title={"clear liked pets"}          containerStyle={{ margin: 10 }}          onPress={this.clearLikedPets}        />      </View>    );  }}function mapStateToProps(state) {  return {    token: state.auth.token  };}ReviewSetting.propTypes = {};export default connect(mapStateToProps, actions)(ReviewSetting);