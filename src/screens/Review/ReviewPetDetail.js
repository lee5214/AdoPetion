import React, { Component } from "react";import { Dimensions, View } from "react-native";import { connect } from "react-redux";import PetDetail from "../../components/Common/PetDetail";import MaterialIcons from 'react-native-vector-icons/MaterialIcons';const { height: SCREEN_HEIGHT, width: SCREEN_WIDTH } = Dimensions.get("window");class ReviewPetDetail extends Component {  static navigationOptions = ({ navigation }) => ({    title: navigation.getParam("petObj").name,    titleFontSize: 30,    headerLeft: (      <MaterialIcons        name="keyboard-arrow-left"        size={30}        type="materialicons"        style={{ paddingLeft: 20 }}        onPress={() => {          navigation.goBack()        }}      />    )  });  goOrg=()=>{    this.props.navigation.navigate('reviewScreen')  }  toOrgDetail = orgID => {    this.props.navigation.navigate("reviewOrgDetail",{orgID:orgID});  };  render() {    //console.log(this.props.navigation.getParam("petID"));    //const id = this.props.navigation.getParam("petID");    const petObj = this.props.navigation.getParam("petObj");    return (      <View style={{ flex: 1, marginTop: 0}}>          <PetDetail petObj={petObj} toOrgDetail={this.toOrgDetail} />      </View>    );  }}mapStateToProps = state => {  return {    likedPets: state.likedPets  };};export default connect(mapStateToProps, null)(ReviewPetDetail);