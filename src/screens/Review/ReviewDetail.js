import React, { Component } from "react";import { Dimensions, Image, ScrollView, Text } from "react-native";import { connect } from "react-redux";const { height: SCREEN_HEIGHT, width: SCREEN_WIDTH } = Dimensions.get("window");class ReviewDetail extends Component {  render() {    console.log(this.props.navigation.getParam("petID"));    const id = this.props.navigation.getParam("petID");    const arr = this.props.likedPets;    const data = arr.filter(pet => {      return pet.animalID === id;    });    console.log("data", data);    const { name, pictures } = data[0];    return (      <ScrollView style={{ flex: 1 }}>        <Image source={{ uri: pictures[0].fullsizeUrl }} style={{ width: SCREEN_WIDTH, height: 300 }} />        <Text style={{ alignSelf: "center" }}>{name}</Text>      </ScrollView>    );  }}mapStateToProps = state => {  return {    likedPets: state.likedPets  };};export default connect(mapStateToProps, null)(ReviewDetail);