import React, { Component } from "react";import { connect } from 'react-redux';import { View } from "react-native";import PetDetail from "../../components/Common/PetDetail";import MaterialIcons from 'react-native-vector-icons/MaterialIcons';class ExplorePetDetail extends Component {  static navigationOptions = ({ navigation }) => ({    title: navigation.getParam("petObj").name,    titleFontSize: 30,    headerLeft: (      <MaterialIcons        name="keyboard-arrow-left"        size={30}        type="materialicons"        style={{ paddingLeft: 20 }}        onPress={() => {          navigation.goBack()        }}      />    ),  });  toOrgDetail = orgID => {    this.props.navigation.navigate("exploreOrgDetail",{orgID:orgID});  };  render() {    const petObj = this.props.navigation.getParam("petObj");    return (      <View style={{ flex: 1}}>        <PetDetail petObj={petObj} toOrgDetail={this.toOrgDetail}/>      </View>    );  }}const mapStateToProps = state => {  return {    petsNearby: state.petsNearby,    petsByOrg: state.petsByOrg  };};export default connect(mapStateToProps,null)(ExplorePetDetail);