import React, { Component } from "react";import { connect } from "react-redux";import { Animated, Dimensions } from "react-native";import ExploreForm from './ExploreForm';const { width: SCREEN_WIDTH } = Dimensions.get("window");class ExploreScreen extends Component {  static navigationOptions = ({ navigation }) => ({    drawerLockMode: "locked-open",    title: "Explore",    tabBarLabel: "Explore",  });  cardOnPress = pet => {    this.props.navigation.navigate("exploreForm", { petObj: pet });  };  render() {    return (      <Animated.View style={{ overflow: "hidden", flex: 1 }}>        <ExploreForm navigation={this.props.navigation}/>        {/*<ScrollView>          <Button title={'Input Search Conditions'} onPress={this.cardOnPress}/>          <ExploreForm navigation={this.props.navigation}/>        </ScrollView>*/}      </Animated.View>    );  }}const mapStateToProps = state => {  return {    likedPets: state.likedPets    //orgsDetailList: state.orgsDetailList  };};export default connect(mapStateToProps, null)(ExploreScreen);