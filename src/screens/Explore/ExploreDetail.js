import React, { Component } from "react";import { Dimensions, View } from "react-native";import { connect } from "react-redux";import PetDetailList from "../../components/Common/PetDetailList";const { height: SCREEN_HEIGHT, width: SCREEN_WIDTH } = Dimensions.get("window");class ExploreDetail extends Component {  static navigationOptions = ({ navigation }) => ({    title: navigation.getParam("petObj").name,    titleFontSize: 30  });  render() {    //console.log(this.props.navigation.getParam("petID"));    //const id = this.props.navigation.getParam("petID");    const petObj = this.props.navigation.getParam("petObj");    return (      <View style={{ flex: 1, marginTop: 0}}>          <PetDetailList petObj={petObj} />      </View>    );  }}mapStateToProps = state => {  return {    likedPets: state.likedPets  };};export default connect(mapStateToProps, null)(ExploreDetail);