import React, { Component } from "react";import { connect } from "react-redux";import { Button, Text, View } from "react-native";import * as actions from "../actions";import Icon from 'react-native-vector-icons/MaterialIcons';class SettingScreen extends Component {  static navigationOptions = ({navigation})=>({    title: "Setting",    tabBarIcon: ({ tintColor }) => {      return (        <Icon name="settings" color={tintColor} size={20} type="materialicons" />      );    },  });  onLogout = () => {    this.props.facebookLogout();    this.props.navigation.navigate("welcome");  };  clearLikedPets = () => {    actions.clearLikedPets();    this.props.navigation.navigate('review')  };  render() {    return (      <View>        <Text>SettingScreen</Text>        <Text>Token: {this.props.token}</Text>        <Button title={"logout"} onPress={this.onLogout} />        <Button title={"clear liked pets"} onPress={this.clearLikedPets} />      </View>    );  }}function mapStateToProps(state) {  return {    token: state.auth.token  };}SettingScreen.propTypes = {};export default connect(mapStateToProps, actions)(SettingScreen);